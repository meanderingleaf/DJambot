export default class SoloCritique {
    //solo critique steps
    beginSoloCritique() {
        //check to see if a critique tracker exists
        //if not.. 
            //create one
        //step the tracker to the new one
    }

    stepFowardSoloCritique() {
        //get the current critique
        //step forward through it
        //display the next prompt
        //Begin the wait
    }
}